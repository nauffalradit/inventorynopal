@extends('layouts.app')
@section('content')
<h1>{{ $order->number }}</h1><p class="muted">{{ $order->customer_name }} · {{ $order->customer_email }}</p><section class="card"><table><thead><tr><th>Barang</th><th>Harga</th><th>Qty</th><th>Subtotal</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td>Rp {{ number_format($item->unit_price,0,',','.') }}</td><td>{{ $item->quantity }}</td><td>Rp {{ number_format($item->subtotal,0,',','.') }}</td></tr>@endforeach</tbody></table><div class="actions" style="justify-content:space-between"><strong>Total: Rp {{ number_format($order->total_amount,0,',','.') }}</strong>@if($order->status==='pending')@php($payment = $order->payments->last()) @if($payment?->checkout_url)<a class="btn primary" href="{{ $payment->checkout_url }}" target="_blank" rel="noopener">1. Buka DOKU Checkout</a><a class="btn" href="https://sandbox.doku.com/integration/simulator/" target="_blank" rel="noopener">2. Buka Simulator DOKU</a><span class="muted">Gunakan invoice: <strong>{{ $order->number }}</strong></span>@else<form method="post" action="{{ route('orders.pay',$order) }}">@csrf<button class="btn primary">Buat Pembayaran DOKU</button></form>@endif @endif</div></section>
@endsection
